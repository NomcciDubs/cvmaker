{
  "name": "nomcci-cvmaker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev:sqlite": "docker compose --env-file compose.sqlite.env.example --profile sqlite up --build",
    "dev:mysql": "docker compose --env-file compose.mysql.env.example --profile mysql up --build",
    "dev:sqlite:ollama": "docker compose --env-file compose.ollama.env.example --profile sqlite --profile ollama up --build",
    "dev:mysql:ollama": "docker compose --env-file compose.ollama.env.example --profile mysql --profile ollama up --build",
    "dev:down": "docker compose --profile sqlite --profile mysql --profile ollama down",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
